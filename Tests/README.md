# Test organization

- `Unit` covers view models and services without rendering application views.
- `Behavior` covers input, commands, and editor or window interactions.
- `Infrastructure` contains shared test applications, hosts, fakes, and adapters.

Add tests to the narrowest matching feature folder.
